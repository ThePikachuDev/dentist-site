---
// src/components/ContactForm.astro
---

<style>
  #pro-submit-btn {
      font-family: 'Syncopate', sans-serif;
      transition: all cubic-bezier(0.19, 1, 0.22, 1) 0.5s;
      position: relative;
      overflow: hidden;
    }
    
    #pro-submit-btn:hover {
      color: white;
    }
    
    #btn-cover {
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background-color: black;
      z-index: 0;
      transition: all 0.5s ease;
    }
    
    #pro-submit-btn:hover #btn-cover {
      left: 0;
    }
    
    #btn-arrow {
      transition: all cubic-bezier(0.19, 1, 0.22, 1) 0.5s;
    }
    
    #pro-submit-btn:hover #btn-arrow {
      transform: rotate(90deg);
      filter: invert(1);
    }


  /* Subtle animations */
  .animate-fade-in {
    animation: fadeIn 0.8s ease-out forwards;
  }
  
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }
  
  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
  
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
  }
  
  /* Form styles */
  .form-input {
    transition: all 0.3s ease;
    box-shadow: 0 1px 2px rgba(0,0,0,0.05);
  }
  
  .form-input:focus {
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2);
    border-color: #3b82f6;
  }
  
  .submit-btn {
    transition: all 0.3s ease;
    box-shadow: 0 4px 6px rgba(59, 130, 246, 0.1);
  }
  
  .submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 12px rgba(59, 130, 246, 0.15);
  }
  
  .contact-card {
    transition: all 0.3s ease;
    box-shadow: 0 4px 6px rgba(0,0,0,0.05);
  }
  
  .contact-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 15px rgba(0,0,0,0.1);
  }
</style>

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
  <div class="text-center mb-16">
    <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4 animate-fade-in" style="animation-delay: 0.1s;">
      Contact US
    </h2>
    <p class="text-lg text-gray-600 max-w-2xl mx-auto animate-fade-in" style="animation-delay: 0.2s;">
      Have questions or need to schedule an appointment? Reach out to us and our team will get back to you promptly.
    </p>
  </div>

  <div class="flex flex-col lg:flex-row gap-12 items-start">
    <!-- Contact Information -->
    <div class="w-full lg:w-1/2 space-y-8">
      <div class="contact-card bg-white p-8 rounded-xl border border-gray-100 animate-fade-in" style="animation-delay: 0.3s;">
        <h3 class="text-2xl font-bold text-gray-900 mb-6">Our Information</h3>
        
        <div class="space-y-6">
          <!-- Phone -->
          <div class="flex items-start">
            <div class="flex-shrink-0 bg-blue-50 p-3 rounded-lg">
              <svg class="h-6 w-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
              </svg>
            </div>
            <div class="ml-4">
              <p class="text-sm font-medium text-gray-500">Phone</p>
              <a href="tel:7087429669" class="text-lg font-semibold text-gray-900 hover:text-blue-600 transition-colors">+91 7087429669</a>
            </div>
          </div>
          
          <!-- Email -->
          <div class="flex items-start">
            <div class="flex-shrink-0 bg-green-50 p-3 rounded-lg">
              <svg class="h-6 w-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
              </svg>
            </div>
            <div class="ml-4">
              <p class="text-sm font-medium text-gray-500">Email</p>
              <a href="mailto:contact@dentalclinic.com" class="text-lg font-semibold text-gray-900 hover:text-green-600 transition-colors">contact@dentalclinic.com</a>
            </div>
          </div>
          
          <!-- Location -->
          <div class="flex items-start">
            <div class="flex-shrink-0 bg-purple-50 p-3 rounded-lg">
              <svg class="h-6 w-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
              </svg>
            </div>
            <div class="ml-4">
              <p class="text-sm font-medium text-gray-500">Location</p>
              <p class="text-lg font-semibold text-gray-900">Dr. Ved Gupta Hospital, Mall Road, Amritsar</p>
              <p class="text-gray-500">Kmaxx American Hospital</p>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Office Hours -->
      <div class="contact-card bg-white p-8 rounded-xl border border-gray-100 animate-fade-in" style="animation-delay: 0.4s;">
        <h3 class="text-2xl font-bold text-gray-900 mb-6">Office Hours</h3>
        <ul class="space-y-4">
          <li class="flex justify-between">
            <span class="text-gray-600">Monday - Friday</span>
            <span class="font-medium">9:00 AM - 6:00 PM</span>
          </li>
          <li class="flex justify-between">
            <span class="text-gray-600">Saturday</span>
            <span class="font-medium">9:00 AM - 2:00 PM</span>
          </li>
          <li class="flex justify-between">
            <span class="text-gray-600">Sunday</span>
            <span class="font-medium">Emergency Only</span>
          </li>
        </ul>
      </div>
    </div>
    
    <!-- Contact Form -->
    <div class="w-full lg:w-1/2 animate-fade-in" style="animation-delay: 0.5s;">
      <form class="bg-white p-8 rounded-xl border border-gray-100 shadow-sm">
        <div class="space-y-6">
          <div>
            <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Full Name</label>
            <input 
              type="text" 
              id="name" 
              name="name" 
              required
              class="form-input w-full px-4 py-3 rounded-lg border border-gray-200"
              placeholder="Your name"
            />
          </div>
          
          <div>
            <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              required
              class="form-input w-full px-4 py-3 rounded-lg border border-gray-200"
              placeholder="your@email.com"
            />
          </div>
          
          <div>
            <label for="phone" class="block text-sm font-medium text-gray-700 mb-1">Phone Number</label>
            <input 
              type="tel" 
              id="phone" 
              name="phone" 
              class="form-input w-full px-4 py-3 rounded-lg border border-gray-200"
              placeholder="+91 1234567890"
            />
          </div>
          
          <div>
            <label for="service" class="block text-sm font-medium text-gray-700 mb-1">Service Needed</label>
            <select 
              id="service" 
              name="service" 
              class="form-input w-full px-4 py-3 rounded-lg border border-gray-200"
            >
              <option value="">Select a service</option>
              <option value="cosmetic">Cosmetic Dentistry</option>
              <option value="pediatric">Pediatric Dentistry</option>
              <option value="root-canal">Root Canal Treatment</option>
              <option value="cleaning">Teeth Cleaning</option>
              <option value="other">Other</option>
            </select>
          </div>
          
          <div>
            <label for="message" class="block text-sm font-medium text-gray-700 mb-1">Your Message</label>
            <textarea 
              id="message" 
              name="message" 
              rows="4"
              class="form-input w-full px-4 py-3 rounded-lg border border-gray-200"
              placeholder="How can we help you?"
            ></textarea>
          </div>
          
 <button id="pro-submit-btn" type="submit" class="flex items-center gap-3 px-6 py-3 font-bold border-2 border-gray-800 rounded-lg cursor-pointer">
        <div id="btn-cover"></div>
        <span class="relative z-10">SUBMIT</span>
        <svg id="btn-arrow" class="relative z-10 w-5 h-5 rotate-45" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 448 512">
          <path d="M214.6 41.4c-12.5-12.5-32.8-12.5-45.3 0l-160 160c-12.5 12.5-12.5 32.8 0 45.3s32.8 12.5 45.3 0L160 141.2V448c0 17.7 14.3 32 32 32s32-14.3 32-32V141.2L329.4 246.6c12.5 12.5 32.8 12.5 45.3 0s12.5-32.8 0-45.3l-160-160z"/>
        </svg>
 </button>

 <!--      <button -->
    <!--     id="animated-btn" -->
    <!--     type="submit" -->
    <!--     class="border-2 w-fit h-fit relative flex gap-3 overflow-hidden cursor-pointer rounded-xl px-5 py-4" -->
    <!-- > -->
    <!--     <div -->
    <!--         id="btn-cover" -->
    <!--         class="w-full h-full z-[-1] absolute top-0 -left-100 bg-black" -->
    <!--     > -->
    <!--     </div> -->
    <!--     <span>Book Appointment</span> -->
    <!--     <img src="/arrow-up-solid.svg" class="w-6 h-6 rotate-45" alt="" /> -->
    <!-- </button> -->
   </div>
      </form>
    </div>
  </div>
</div>

<!-- Minimal GSAP implementation that won't break -->
<script is:inline>
  if (typeof window !== 'undefined') {
    // Load GSAP from CDN if not already loaded
    if (typeof gsap === 'undefined') {
      const script = document.createElement('script');
      script.src = 'https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.4/gsap.min.js';
      script.onload = initAnimations;
      document.head.appendChild(script);
    } else {
      initAnimations();
    }
    
    function initAnimations() {
      // Only add subtle animations if GSAP is available
      if (typeof gsap !== 'undefined') {
        // Animate form inputs on focus
        document.querySelectorAll('input, textarea, select').forEach(el => {
          el.addEventListener('focus', () => {
            gsap.to(el, { scale: 1.02, duration: 0.2 });
          });
          el.addEventListener('blur', () => {
            gsap.to(el, { scale: 1, duration: 0.2 });
          });
        });
        
        // Button hover effect
        const submitBtn = document.querySelector('.submit-btn');
        if (submitBtn) {
          submitBtn.addEventListener('mouseenter', () => {
            gsap.to(submitBtn, { y: -2, duration: 0.2 });
          });
          submitBtn.addEventListener('mouseleave', () => {
            gsap.to(submitBtn, { y: 0, duration: 0.2 });
          });
        }
      }
    }
  }
</script>
